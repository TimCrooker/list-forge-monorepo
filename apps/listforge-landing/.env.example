# App URL Configuration
# URL to the main application (used for Sign In / Register links)
VITE_APP_URL=https://app.listforge.io

# API URL (if needed in the future)
VITE_API_URL=https://api.listforge.io
